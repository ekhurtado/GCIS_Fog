apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: exist-config
  name: exist-config
spec:
  storageClassName: exist-config
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
status: {}
