version: "2"

services:

  sink-exist:
    build:
      context: ../../../../../Downloads
      dockerfile: Dockerfile_Sink_eXist
    image: ekaitzhara/sink-v2:sink-exist
    container_name: sink-exist
    environment:
      - FUNCTION=storeAssemblyStationData

